const INITIAL_STATE = {
  groceries: [
    {
      
    }
  ],
};

const groceryReducer = (state = INITIAL_STATE, action) => {
  switch (action.type) {
    default:
      return state;
  }
};

export default groceryReducer;
